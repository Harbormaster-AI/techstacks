name: #appName GitHub Actions CI

on:
  push:
    branches: [ $default-branch ]
  pull_request:
    branches: [ $default-branch ]

jobs:
  build:

    runs-on: #GitHubActions_RunsOn()

#GitHubActions_Strategy()

    steps:
#GitHubActions_Steps_Uses()
#GitHubActions_Steps()
